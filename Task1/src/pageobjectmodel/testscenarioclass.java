package pageobjectmodel;

import java.io.IOException;
import java.util.Iterator;
import java.util.List;

import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;

public class testscenarioclass {

	public static void main(String[] args) throws InterruptedException {
		baseclass b = new baseclass();
		//WebDriver driver = b.usechromeandlogin();
	b.getproperty();
		//b.setproperty();
		//pageclass p = new pageclass();
		//List<WebElement> act = p.capturealloptions(driver);
		//Expectedresult er = new Expectedresult();
		//List<String> expresult = er.expectedresult1();
		/*
		 * for (int i = 0; i < (expresult.size()); i++) {
		 * System.out.println("The actual size is : " + expresult.size()); if
		 * (expresult.contains(act.get(i).getText())) {
		 * System.out.println("The element is found: " + act.get(i).getText()); } else {
		 * System.out.println("The element is not found: " + act.get(i).getText()); } }
		 */
	}
}
